<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <ir.pkokabi.pdialogs.Selector.Selector
            android:id="@+id/testCs"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:icon="@drawable/ic_brand"
            app:text="Show Message"
            app:textSize="14sp" />

        <ir.pkokabi.pdialogs.CheckBox.CheckBox
            android:id="@+id/cabinFilter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            app:text="abc" />

        <android.support.v7.widget.AppCompatButton
            android:id="@+id/showValueBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Show Value" />


    </LinearLayout>

</layout>